package org.example;
/**
 * Here we have a VW child class, just a type/brand of Car.
 * We can add object VW type.
 */
public class VW extends Car {
    public VW(int availableFuel, String chassisNumber) {

        super(availableFuel, chassisNumber);
    }
}
